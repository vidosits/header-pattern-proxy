displayName: Header Pattern Reverse-Proxy
type: middleware

import: github.com/vidosits/header-pattern-proxy

summary: "Reverse proxy traffic based on a pattern of a header's value."

iconPath: images/icon.png
bannerPath: images/icon.png

testData:
  Header: 'X-Forwarded-User'
  Mapping:
    user1: 'http://service1'
    user2: 'http://service2'
